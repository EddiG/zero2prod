name: zero2prod
region: lon1
services:
  - name: zero2prod
    dockerfile_path: Dockerfile
    source_dir: .
    github:
      repo: EddiG/zero2prod
      branch: axum
      deploy_on_push: true
    health_check:
      http_path: /health_check
    http_port: 8000
    instance_count: 1
    instance_size_slug: basic-xxs
